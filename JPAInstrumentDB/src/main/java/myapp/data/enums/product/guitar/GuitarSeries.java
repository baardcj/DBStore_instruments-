package myapp.data.enums.product.guitar;

/*
 * This enum represent permitted values for field in GuitarModel.class. 
 * 
 * This enum is for testing and should contain for now all series.
 * Future work: the guitar series field should only be assigned values consistent with 
 * guitar brand in GuitarModel.
 */

public enum GuitarSeries {
	STANDARD,
	CUSTOM_SHOP,
	REISSUE, 
	VINTAGE, 
	DELUXE,
	AFFORDABLE,
}
